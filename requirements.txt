# Core
pandas>=2.2
sqlalchemy>=2.0
psycopg2-binary>=2.9
python-dotenv>=1.0
tabulate>=0.9  # required by df.to_markdown() in llm/chatbot.py
# torch 2.2.2 (latest available for Python 3.11 on x86_64 macOS) was compiled
# against NumPy 1.x — NumPy 2.x causes an _ARRAY_API import error at runtime.
numpy>=1.26,<2

# ETL / data quality
openpyxl>=3.1
great-expectations>=0.18  # optional: swap for lightweight custom checks if too heavy
# snowflake-sqlalchemy>=1.6  # optional: only needed for etl/load_snowflake.py
# (run_pipeline.py --target snowflake/both). Commented out by default since
# no Snowflake account is configured in this environment and the module is
# untested against a live connection — uncomment once you have credentials.

# RAG
langchain>=0.2
langchain-anthropic>=0.1
langchain-community>=0.2
chromadb>=0.5
# sentence-transformers 4.x+ requires torch>=2.4, which has no wheel for
# Python 3.11 on x86_64 macOS — pin to 3.x until torch 2.4+ is available.
sentence-transformers>=3.0,<4.0

# LLM
anthropic>=0.30
requests>=2.31  # used for local Ollama backend (LLM_BACKEND=ollama)

# App layer
fastapi>=0.111
uvicorn>=0.30
streamlit>=1.36

# Testing
pytest>=8.2
